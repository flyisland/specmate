# docs/prd/

产品需求文档。

## PRD 状态流转

```
draft -> approved -> obsolete
```

| 状态 | 含义 |
|---|---|
| `draft` | 编写中，不能用于设计或实现决策 |
| `approved` | 已审批，Design Doc 和 Exec Plan 可以引用 |
| `obsolete` | 功能取消或方向废止 |

## 命名规则

`prd-001-<slug>.md` — 三位数 ID，全局递增。
